'use client';

import { VisualEditorPage } from '@/features/visual-editor';

export function VisualFloorPlanView() {
    return (
        <div className="animate-in fade-in duration-500 -mx-4 md:-mx-6">
            <VisualEditorPage />
        </div>
    );
}
