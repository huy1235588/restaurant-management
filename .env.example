# ============================================
# ENVIRONMENT CONFIGURATION - Docker Compose
# ============================================
# This file is used by docker-compose for configuring services
# Copy this file to .env and update the values
# ⚠️ NEVER commit .env file with real secrets to repository

NODE_ENV=development

# ============================================
# DATABASE CONFIGURATION (PostgreSQL)
# ============================================
POSTGRES_USER=restaurant_admin
POSTGRES_PASSWORD=restaurant_password_dev
POSTGRES_DB=restaurant_db
POSTGRES_PORT=5432
# Full DATABASE_URL for server (generated from above values)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# When running in Docker: postgresql://restaurant_admin:restaurant_password_dev@postgres:5432/restaurant_db?schema=public

# ============================================
# REDIS CONFIGURATION
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
# For development: redis://redis:6379
# For production: redis://[PASSWORD]@[HOST]:[PORT]

# ============================================
# SERVER CONFIGURATION
# ============================================
SERVER_PORT=5000
API_VERSION=v1
API_BASE_URL=http://localhost:5000
SERVER_HOST=localhost

# ============================================
# CLIENT CONFIGURATION
# ============================================
CLIENT_PORT=3000
CLIENT_URL=http://localhost:3000
CLIENT_HOST=localhost

# ============================================
# CLIENT - NEXT.js PUBLIC VARIABLES (exposed to browser)
# ============================================
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
NEXT_PUBLIC_API_TIMEOUT=30000
NEXT_PUBLIC_IMAGE_DOMAINS=

# ============================================
# AUTHENTICATION (JWT)
# ============================================
# ⚠️ CHANGE THESE IN PRODUCTION!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-immediately
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
BCRYPT_ROUNDS=10

# ============================================
# NGINX CONFIGURATION (for production with reverse proxy)
# ============================================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ============================================
# STORAGE CONFIGURATION
# ============================================
# Options: 'local' for local file storage, 'cloudinary' for Cloudinary CDN
STORAGE_TYPE=local
# Base URL for file serving (used by local storage)
BASE_URL=http://localhost:3000

# Cloudinary Configuration (for production)
# Sign up at: https://cloudinary.com
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ============================================
# FILE UPLOAD
# ============================================
# Maximum file size (in bytes) - 5MB = 5242880
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM=Restaurant Management <noreply@restaurant.com>

# ============================================
# LOGGING
# ============================================
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# ============================================
# OTHER CONFIGURATION
# ============================================
TZ=Asia/Ho_Chi_Minh
